# react-reveal@1.2.2 declares a peer of react ^15 || ^16, but this project runs
# react 17. The combination works in practice, so accept the legacy resolution
# instead of requiring --legacy-peer-deps on every install.
legacy-peer-deps=true
